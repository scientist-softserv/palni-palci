<%# TODO: Remove header and add appropriate form fields when we have more info %>
<h2>OIDC FIELDS</h2>
<%= f.input :client_id, label: t('hyku.auth_provider.label.client_id'), required: true %>
<%= f.input :client_secret, label: t('hyku.auth_provider.label.client_secret'), required: true %>
<%= f.input :idp_sso_service_url, label: t('hyku.auth_provider.label.idp_sso_service_url') %>