<%# TODO: Remove header and add appropriate form fields when we have more info %>
<h2>SAML FIELDS</h2>
<%= f.input :saml_client_id, label: t('hyku.auth_provider.label.client_id'), required: true %>
<%= f.input :saml_client_secret, label: t('hyku.auth_provider.label.client_secret'), required: true %>
<%= f.input :saml_idp_sso_service_url, label: t('hyku.auth_provider.label.idp_sso_service_url') %>