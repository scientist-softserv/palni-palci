<%= f.input :show_viewer,
            label: "Show PDF.js Viewer",
            required: f.object.required?(:show_viewer),
            as: :boolean,
            input_html: { data: { toggle: 'toggle', onstyle: 'primary' } }
%>
