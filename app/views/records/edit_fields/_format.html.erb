<%# CUSTOM: app/views/records/edit_fields/_format.html.erb  %>
<% formats = Hyrax::FormatService.select_options.map { |format| format.first }.uniq %>
<%= f.input :format,
  collection: formats,
  as: :multi_value_select,
  include_blank: true,
  input_html: { class: 'form-control', multiple: true }
%>