<%# OVERRIDE: Hyrax 3.5.0 to make resource_type a single value select field on the form %>
<%= f.input :resource_type, as: :select,
    collection: Hyrax::ResourceTypesService.select_options,
    input_html: { class: 'form-control', multiple: true, value: f.object.resource_type.try(:first) } %>
