<%# OVERRIDE: Hyrax 3.5.0 to make title a single value on the form %>
<%= f.input :title, as: :string, input_html: { class: 'form-control',
    multiple: true, value: "#{f.object.title.try(:first)}" } %>
