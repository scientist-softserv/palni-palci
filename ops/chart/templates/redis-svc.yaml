---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "hyku.redis.name" . }}
  labels:
    app: {{ template "hyku.name" . }}
    chart: {{ template "hyku.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: redis
spec:
  ports:
    - protocol: TCP
      port: 6379
  selector:
    app: {{ template "hyku.name" . }}
    release: {{ .Release.Name }}
    component: redis
